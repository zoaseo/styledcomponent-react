import React from 'react';
import styles from './Box.module.css';

const Box = () => {
    return (
        <div className={styles.Box}>
            나는 박스입니다.
        </div>
    );
};

export default Box;